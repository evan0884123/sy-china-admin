<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sychina.admin.infra.mapper.AdminUserMapper">
    
    <resultMap id="RM-UserTableModel" type="com.sychina.admin.web.pojo.models.AdminUserTable">
        <id column="id" jdbcType="CHAR" property="id"/>
        <result column="login_name" jdbcType="VARCHAR" property="loginName"/>
        <result column="full_name" jdbcType="VARCHAR" property="fullName"/>
        <result column="sub_departments" jdbcType="CHAR" property="subDepartments"/>
        <result column="role_id" jdbcType="INTEGER" property="roleId"/>
        <result column="mobile" jdbcType="VARCHAR" property="mobile"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="type" jdbcType="CHAR" property="type"/>
        <result column="role_name" jdbcType="VARCHAR" property="roleName"/>
    </resultMap>

    <sql id="CONDITION">
        <where>
            and 1 = 1
            <if test="query.name!=null and query.name !=''">
                and (u.login_name like concat('%', #{query.name}, '%') or u.full_name like concat('%', #{query.name}, '%'))
            </if>
            <if test="query.roleId!=null">
                and u.role_id = #{query.roleId}
            </if>
        </where>
    </sql>

    <select id="findTable" resultMap="RM-UserTableModel">
        select
        u.*,
        r.`name` as role_name
        from admin_user u left join admin_role r on u.role_id = r.id
        <include refid="CONDITION"/>
    </select>

</mapper>